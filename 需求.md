

# 角色设定

你是一个顶级的全栈 Python 开发者和 UI/UX 设计师。请帮我使用 Flask 框架开发一个名为“健身达人”的 Web 应用，并实现以下所有功能。
请自主完成所有文件的创建、代码编写和环境配置，遇到错误自动修复，直到项目可以顺利运行。



# 技术栈要求

- 后端：Python 3, Flask, Flask-SQLAlchemy, Flask-Login, PyMySQL (用于连接 MySQL)
- 数据库：MySQL
  - 数据库连接配置：用户名 `root`，密码 `123456`，端口 `3308`，主机 `localhost` (或 `127.0.0.1`)。
  - 数据库名：`fitness_app`（请在连接前确保创建该数据库，或在代码中包含自动创建数据库的逻辑）。
  - SQLAlchemy URI 示例：`mysql+pymysql://root:123456@127.0.0.1:3308/fitness_app?charset=utf8mb4`
- 前端：HTML5, TailwindCSS（CDN 引入）, 原生 JavaScript
- 图表库：ECharts（CDN 引入）
- 日历库：FullCalendar 或手写 JS 日历

# 执行要求
1. 请先编写一个初始化脚本或直接在 `app.py` 运行时连接 MySQL (端口 3308)。如果 `fitness_app` 数据库不存在，请自动执行 SQL 语句创建它。
2. 安装依赖时，务必记得包含 `PyMySQL` 和 `cryptography`（如果 SQLAlchemy 报错需要的话）。
3. 构建 `models.py` 并使用 Flask-Migrate 或 `db.create_all()` 初始化所有表结构。
  ...（后面的执行要求保持不变）







# 核心功能需求

## 1. 账号模块（登录/注册）
- 实现基础的用户名和密码注册、登录功能。
- 不需要验证码，密码需进行哈希加密（如使用 Werkzeug.security）。
- 登录后跳转至用户仪表盘（Dashboard）。

## 2. 界面与 UI 设计
- 整体风格要求：现代、清新、运动风。使用卡片式布局，配合 TailwindCSS 添加 hover 效果和丝滑的过渡动画。
- 导航栏包含：仪表盘、饮食记录、体重曲线、健康助手、打卡日历。

## 3. 饮食摄入与百度 AI 食物识别
- 页面设计：分为“早餐”、“午餐”、“晚餐”三个版块。
- 拍照识别：提供图片上传功能，后端调用**百度 AI 开放平台-菜品识别 API**。
  - API Key: `azpYF3xTeYg2HU8GQc9avyMI`
  - Secret Key: `ro3w0ImdkNIzZUPzUftMDRigjiP8R5Jh`
  - 逻辑：先通过上述 Key 获取 access_token，然后调用 `https://aip.baidubce.com/rest/2.0/image-classify/v2/dish`，提取返回的菜品名称和卡路里（`calorie`），并将提取到的热量自动填入对应的早/中/晚记录中。
- 汇总功能：在页面顶部总结当天的“总热量摄入”。

## 4. 消耗与热量差计算
- 基础消耗（BMR）：固定为“学生正常学习一天消耗的热量”，设定默认值为 1800 kcal。
- 运动消耗：提供一个输入框，允许用户手动填写“今日运动消耗热量”。
- 核心算法：实时计算并在 UI 上高亮显示“热量差” = (总热量摄入) - (基础消耗 + 运动消耗)。

## 5. 体重追踪与 AI 健康分析
- 体重记录：用户可以每天打卡输入当天的体重（kg）。
- 动态图表：使用 ECharts 渲染该用户的近期体重变化曲线图（折线图，带渐变阴影，界面优美）。导出功能可调用 ECharts 自带的 saveAsImage。
- AI 健康分析：
  - 提取用户最近 7 天的体重数据和热量差数据。
  - 调用智谱 AI API，模型指定使用 `glm-4.5-air` 或 `glm-4-air`。
  - API Key: `1298a8e6745843e3bb2fa3de964cc2b8.hLfhANnzCGHg3b4A`
  - 提示词要求：让 AI 以专业营养师的口吻，分析体重变化趋势和热量摄入情况，并给出 100 字左右的健康建议。将结果展示在图表下方。

## 6. 健康小助手（AI 对话）
- 在页面右下角或单独页面提供一个 Chat UI。
- 接入智谱 AI API (模型: `glm-4.5-air` 或 `glm-4-air`，使用相同的 API Key)。
- 预设 System Prompt：“你是一个贴心的健康与健身助手，回答应当简短、专业、有鼓励性。”
- 用户可自由提问，实现上下文对话。

## 7. 打卡与日历功能
- 提供一个“今日打卡”按钮。
- 渲染一个优美的月度日历。
- 关联数据库中的打卡记录，如果该用户在某一天点击了打卡，日历对应日期的数字下方需要显示一个小圆点作为标注。
- 统计并显示“本月累计打卡天数”。

# 执行要求
1. 请先设计并创建 `models.py` 中的数据库结构（User, DietRecord, WeightRecord, CheckInRecord）。
2. 构建 `app.py` 路由。
3. 编写所有对应的 HTML 模板文件，注重 TailwindCSS 的类名运用。
4. 提供独立的百度 AI 和智谱 AI 调用工具函数。
5. 自动运行并检查报错。无需每次询问我，直接完成项目的初始可运行版本。